name: Deploy

on:
  push:
    branches:
      - 'lowe-dev'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install
        env:
          POSTGRES_URL: ${{ secrets.POSTGRES_URL }}
          AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
          AUTH_URL: ${{ secrets.AUTH_URL }}

      - name: Genrate Deployment Package
        run: zip -r nodejs-app-build.zip .

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v21
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY }}
          aws_secret_key: ${{ secrets.AWS_SECRET }}
          application_name: nextjs-app2
          environment_name: Nextjs-app2-env
          version_label: ${{ github.sha }}
          region: eu-north-1
          deployment_package: nodejs-app-build.zip

    #   - uses: actions/checkout@v4

    #   - name: Generate deployment packageqwe
    #     env:
    #       POSTGRES_URL: ${{ secrets.POSTGRES_URL }}
    #       AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
    #       AUTH_URL: ${{ secrets.AUTH_URL }}
    #     run: npm run next-install && npm run generate-zip

    #   - name: Get timestamp
    #     uses: gerred/actions/current-time@master
    #     id: current-time

    #   - name: Run string replace
    #     uses: frabert/replace-string-action@master
    #     id: format-time
    #     with:
    #       # replaces '[:\.]+' in the timestamp with '-'
    #       pattern: '[:\.]+'
    #       string: '${{ steps.current-time.outputs.time }}'
    #       replace-with: '-'
    #       flags: 'g'

    #   - name: Deploy to EB
    #     uses: einaregilsson/beanstalk-deploy@v21
    #     with:
    #       aws_access_key: ${{ secrets.AWS_ACCESS_KEY }}
    #       aws_secret_key: ${{ secrets.AWS_SECRET }}
    #       application_name: nextjs-app2
    #       environment_name: Nextjs-app2-env
    #       version_label: nextjs-app-${{ steps.format-time.outputs.replaced }}
    #       region: eu-north-1
    #       deployment_package: deploy.zip
